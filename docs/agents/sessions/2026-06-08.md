# Session 2026-06-08

## Mobile UX Fast Path

Request:

- Re-check fresh design docs/review.
- Pick the most meaningful visible design fix.

Context:

- Followed the required agent start route and autosync.
- Fresh plan source: `.planning/phases/PHASE_4_2_MOBILE_UX_LOBBY_STYLE_PLAN.md`.
- Selected Segment C / First Game Fast Path as the highest visible UX fix:
  mandatory onboarding and room management were slowing first play.

Actions:

- Added `apps/web/src/lib/quickStartRoster.ts` for a reusable local bot-backed starter roster.
- Changed store initial screen so persisted `onboardingComplete` opens `lobby` instead of mandatory onboarding.
- Changed first onboarding action:
  - `Играть сейчас` saves onboarding completion and starts a local `Long 25` classic match.
  - `Как играть` opens the existing tutorial/rules path.
- Kept rules/tutorial available as the optional `rules` screen.
- Added a prominent lobby CTA:
  - `Играть`
  - `Long 25 · с ботами`
  above room/join/create controls.

Verification:

- `npm run build --workspace=apps/web`
- `npm exec vite preview -- --host 127.0.0.1 --port 4173`
- Mobile screenshots:
  - `/tmp/dyor-onboarding-fast-ready.png`
  - `/tmp/dyor-lobby-fast-play-ready.png`

Notes:

- In-app Browser JS bridge was not available in this session; Playwright CLI screenshots worked.
- A click-level Playwright spec could not run because the npx CLI was available but the test module
  was not resolvable from the temporary spec. Re-run click automation when local Playwright test
  tooling is installed or the Browser bridge is available.

## Lobby CTA Placement Correction

Request:

- Preserve the branded lobby composition: animated pet, character pose, interior, and central room space.
- Move the new fast-start CTA out of the central scene.
- Put the CTA where `Сейчас в сети` was, and push `Сейчас в сети` below it.

Actions:

- Moved `Играть · Long 25 · с ботами` below the room/join/create controls.
- Restored the central lobby area so the pet/interior composition stays visible.
- Kept `Сейчас в сети` below the CTA so it remains available after scrolling.

Verification:

- `npm run build --workspace=apps/web`
- `npm exec vite preview -- --host 127.0.0.1 --port 4173`
- Mobile screenshot: `/tmp/dyor-lobby-fast-play-moved.png`

## Online Shared Cards + Reactions

Request:

- In 25-round online play, make the main branch show the same card to all players.
- Keep the current different-card behavior as a separate mode.
- Show online character reactions above the avatar miniature for all players.
- Review card content and add a one-per-session immunity idea for bad cards.
- Read deploy docs and avoid breaking the working Cloudflare/Fly shape.

Actions:

- Read `DEPLOY.md`: kept the existing single-port Fastify + `/ws` server contract and Vite env URLs intact.
- Added server `cardMode: shared | individual`.
  - `shared` is the online default and opens `intent_window` so all players submit against one visible `currentCardId`.
  - `individual` preserves the old sequential per-player card branch.
- Updated lobby room mode controls:
  - `Общие` = shared same-card mode;
  - `Личные` = old different-card branch;
  - `Драфт` stays available.
- Wired in-match reactions through WebSocket:
  - local reactions send `{ type: 'reaction', playerId, label }`;
  - incoming `reaction` broadcasts render above the matching player miniature, including self.
- Added `prot-crisis-immunity` card and deterministic engine behavior:
  - adds `crisis_immunity`;
  - next negative crisis choice rolls 50%;
  - token is consumed on roll and cannot be bought again after use in the same match.
- Tightened several crisis-card descriptions/hints so choices explain the tradeoff.
- Added tests for immunity block/rebuy rejection and updated card distribution expectations.

Verification:

- `npm test` - 124/124 passed.
- `npm run typecheck:server` - passed.
- `npm run typecheck` - passed.
- `npm run build --workspace=apps/web` - passed.
- Local WS smoke test, shared mode:
  - two clients joined one room;
  - both received the same `currentCardId`;
  - both submitted intents;
  - server advanced to round 2 and reopened `intent_window`.
- Local WS smoke test, individual mode:
  - room started in `decision`;
  - after one player pass, round advanced while staying in sequential `decision`.

Notes:

- In-app Browser runtime was not callable in this session, so visual browser clicking was not completed.
- Local dev server was started with `npm run dev` for manual verification at `http://localhost:5173/`.

# Session 2026-08-04 — account identity and honest financial actions

## Account-scoped first run

- The in-match tutorial was previously one browser-wide flag. It is now keyed by
  `Telegram.WebApp.initDataUnsafe.user.id`.
- The player profile/progression store is scoped by the same Telegram id, so a
  second account on one device cannot inherit another account's completed
  onboarding, nickname, inventory or progress.
- The first Telegram account opening the account-aware release receives the old
  unscoped profile and tutorial flag once. The legacy keys are removed, allowing
  later accounts to start clean.
- Outside Telegram, local browser behavior stays unchanged. `?tour=1` continues
  to force the ten-step match tutorial for QA.

## Honest transport feedback

The WebSocket client already reconnected and resumed a room, but store actions
often ignored `send() === false` and immediately returned success. The UI could
therefore say an asset was bought, a deal accepted or a reaction sent when no
command left the phone.

Boolean economy operations now propagate transport failure. Personal offers,
table help, futures, incoming deals and card submission no longer claim success
on a dead socket. The main decision CTA blocks during reconnect, asks the player
to repeat the decision, and triggers an immediate reconnect. Stale financial
commands are intentionally not queued: replaying a purchase or sale after the
state changes would create duplicate or outdated decisions.

## Irreversible action review

- Added one shared warning dialog for asset sale, asset transfer and revenue
  sharing.
- Review data is captured at the moment of intent and shows asset, recipient,
  sale proceeds and monthly-flow change.
- Cancel is focused first; Escape cancels only the review and no longer closes
  the underlying bottom sheet.
- The profile's quick sale uses the same engine resale multiplier and shows the
  exact `$720` cap for a `$1,000` Coffee asset.

## Browser-discovered engine/UI mismatch

The 402×874 rendered market enabled Coffee at `$1,000`, but clicking it produced
`Недостаточно наличных` while the player had `$2,000+`. The real reason was a
canonical payload rejection: the registry requires upkeep `$10`, `$20` and `$50`
for Coffee, Kiosk and Studio, while the market omitted upkeep and sent zero.

The market now sends the complete canonical payload. Browser replay proved:

- Coffee purchase reduced cash by `$1,000`;
- the asset appeared in the profile ledger;
- passive income increased by `$200` and upkeep by `$10`;
- the market displays asset flow as `+$190/мес` with the `+$200 −$10`
  breakdown;
- sale review showed `+$720` cash and `−$190/мес` asset flow.

## Learning-path evidence

- Timer remained `00:90 → 00:90` over 2.2 seconds during the tutorial.
- The tutorial advanced to `Люди за столом` without ending the pause.
- Tapping a highlighted portrait opened the real player profile.
- Sending `OK` closed the profile, rendered the reaction on that player and
  returned to tutorial step 6/10.
- The first tooltip and shared sheet headers remained below the reserved Telegram
  top strip in the 402×874 mobile frame.

## Verification

- Web TypeScript: PASS.
- Server TypeScript: PASS.
- Engine: 150/150 PASS.
- Production web build: PASS.
- Browser: tutorial pause/profile/reaction, canonical market purchase and sale
  review PASS at 402×874.

## Process observations for Studio

- Start UI audits from a real economic path (`visible price → command payload →
  registry validation → state delta → feedback`). Source-level agreement on names
  alone did not catch the upkeep mismatch.
- Do not add a generic offline command queue for financial actions. Reconnect the
  room and make the user reconfirm against the restored state.
- Keep one scoped accessibility snapshot per state and query exact controls after
  that. Repeated full dumps and complete build asset manifests are the largest
  avoidable context costs in this workflow.
- Separate stable product rules in Second Brain from chronological evidence in
  session logs; this keeps future agents from rereading the entire history.

## Attention hierarchy and month transition

- Added one modal-layer contract for bottom sheets, confirmations, negotiation,
  lobby rooms and reaction pickers. Only the top layer owns focus, Escape and
  Telegram Back; focus returns to the opening control and nested confirmations
  no longer close the sheet behind them.
- Daily reward now uses the same Telegram-safe sheet as bank, market, labor,
  pets and the event log. Full-screen deal/futures/settings routes use a real
  Back action instead of a top-right close affordance competing with Telegram.
- Action-required offers use one labelled decision tray. It sits above transient
  notices but below modal/tutorial/turn layers, has bounded mobile height and is
  hidden while another modal owns attention.
- The tutorial is suspended while any real sheet, confirmation, route menu,
  reaction picker, offer builder or round transition is active.
- Labor's worker review now uses the shared confirmation and keeps the candidate
  portrait plus exact upfront/monthly salary and bonus. Shared confirmations are
  portalled to `document.body`, so nested sale/transfer/hire dialogs cannot be
  trapped by a transformed sheet or inherit its coordinate system.
- A submitted choice now has an explicit close → night/settlement → new month
  sequence before the next card is exposed. In multiplayer, the night screen
  never reads the previous round's settlement while the server snapshot is late.
- The night phase now plays an itemized money ledger: work, passive income,
  purchased assets with their upkeep, recurring life/staff/debt/tax costs and
  the residual impact of the round's decision/events. Rows reveal in sequence,
  then `Пришло`, `Ушло` and the wallet-reconciled round total appear. This makes
  bought assets visibly affect every later month instead of becoming hidden stats.
- Browser evidence at 402×874: main card, bank, market, labor, pets, daily reward,
  event log and rooms browser stayed within the viewport; shared sheets begin at
  y=122, the daily compact sheet at y=246 and rooms are centered at y=314.

## Read-only futures math audit

The core direction formula is correct: notional is `margin × leverage`; long P&L
is `(mark − entry) × notional / entry`, short reverses the price delta. The
surrounding settlement contract is not ready to trust:

- one-round price movement cannot reach the current liquidation threshold, so
  liquidation warnings are effectively decorative;
- just before liquidation an ordinary loss can exceed margin, then become
  smaller when the next worse tick triggers liquidation;
- all-in margin hides cash from negative monthly flow and funding, producing an
  exploitable wallet result while the event log claims the fee was paid;
- the client receives deterministic RNG state and can predict the next server
  tick; the decorative chart is unrelated to the authoritative price;
- UI reports gross P&L while the wallet receives net P&L, and card-opened versus
  futures-screen positions use different liquidation rules.

This audit made no futures changes. Fix the settlement ledger and exact tests as
a separate engine task; do not tune win rates until accounting is internally
consistent and visible to the player.

## Verification for this pass

- Web TypeScript: PASS.
- Server TypeScript: PASS.
- Engine: 150/150 PASS.
- Production web build: PASS (existing large-chunk warning remains).
- Browser: iPhone 16 Pro viewport 402×874, shared sheet geometry, Daily, lobby
  rooms, settings return, Escape ownership, nested Labor confirmation and
  itemized night ledger PASS.
- Production: canonical `cashflow-mini-app-game.pages.dev` opened the new
  first-card tutorial and shared Labor confirmation. The alert dialog sits at
  `350–862px` inside an `874px` viewport, is portalled under `BODY`, focuses
  Cancel first, and Escape closes it while keeping the Labor sheet open.

## Additional Studio/process notes

- UI audits should first produce an attention-owner matrix: surface type,
  priority, safe-area rule, focus owner, Back/Escape behavior and what must pause
  underneath. This exposed more defects than reviewing z-index values alone.
- Reuse one modal primitive and one notice lane as measurable contracts; flag any
  new fixed overlay that bypasses them during preflight.
- For browser work, retain one mobile tab and take a scoped accessibility snapshot
  only after a state change. Geometry can be checked in one bounded DOM query;
  repeated full snapshots and build asset manifests consume context without
  improving confidence.
- Keep economy audit separate from visual audit, but record the join point
  (`UI claim → command → authoritative ledger → feedback`). This prevented a
  correct-looking futures screen from being mistaken for correct accounting.
- Studio polling was repaired after the measurement: DIOR now sends bounded run
  summaries instead of duplicated task prompts/results, refreshes CLI/Git
  diagnostics in the background and serves a stale complete snapshot while a
  refresh runs. Cold state dropped from about `885 KB / 18–23s` to
  `257 KB / 1.6s`; refreshed polls measured `278 KB / 6–18ms`. The July task was
  preserved as `stopped` with an append-only cancellation event, so resuming the
  workspace cannot launch obsolete work.

## Unified window system and iPhone 16 Pro replay

- Added one full-screen route header contract for rules, settings, shop,
  character editor, deals, Futures and recap. The first interactive row begins
  below the Telegram-owned strip; every route exposes the same 44px Back action
  and restrained title/subtitle hierarchy.
- Shared sheets and confirmations now use common window surface, scrim, border,
  radius, shadow and motion tokens. Table tools and the personal-card-sale
  picker joined the modal focus/Back stack instead of behaving as free-floating
  popovers.
- Settings now has a visible return path. The character editor remembers whether
  it was opened from shop, lobby or the table and returns there.
- Raised the remaining high-frequency controls to the 44px touch contract:
  confirmation preview, market filters, hire actions, bank presets/actions,
  negotiation swap/side-pay, lobby mode/round controls, profile page dots and
  profession details.
- Browser replay found a real stacking bug in the lobby: the decorative pet
  appeared over the collection row but was below it in hit testing, so a tap
  could open the profile/interior instead of pets. The room pet is now decorative
  and noninteractive; the unobstructed `Питомцы` collection tile is the single
  control. Replay opened `Питомец в лобби` correctly.
- At 402×874, editor content ended at y=842 inside the 874px viewport; shared
  sheets began at y=122; shop/settings headers reserved y=0–166 for Telegram and
  local route chrome; there was no horizontal overflow.
- Verified paths: lobby and rooms; all 15 character portraits with zero broken
  images; player profile and LOL reaction; bank; market; labor; pets; rules;
  settings return; shop; editor; cashflow; confirmation preview. The selected
  AI-template card preview reconciled the visible financial transition exactly.

### Verification

- `npm run typecheck`: PASS.
- `npm run typecheck:server`: PASS.
- Engine tests: 150/150 PASS.
- `npm run build --workspace=apps/web`: PASS; existing >500KB chunk warning only.
- Browser console: zero application errors. Only Telegram WebApp 6.0 capability
  warnings appeared in the local harness.

### Studio/process observation

- The UI-only Studio task was blocked by a fresh root-scoped engine dependency
  despite a verified product checkpoint. Creating design/balance/gameplay/engine
  no-op tasks would have spent several contexts without changing UI risk, so the
  queued task was stopped append-only and the scoped owner work continued.
- Browser evidence again paid for itself: source review showed an `onClick` on
  the lobby pet, but `elementFromPoint` proved the higher carousel intercepted
  the visible pet. Future audits should pair one accessibility snapshot with one
  bounded geometry/hit-test query rather than trust either source or screenshot
  alone.

## Center-only month settlement

- Removed the full-screen day/night sky and background replacement. The table,
  players, market and HUD now remain spatially fixed while only one local ledger
  card animates in the center.
- Kept the center card mounted across closing, settlement and next-month phases,
  avoiding a phase-by-phase layout remount and visible screen jump.
- Extended the readable settlement hold from 1.8 seconds to an adaptive 4–5.2
  seconds. Ledger rows reveal at 210ms intervals before totals and the wallet
  delta, so negative cashflow is as legible as positive cashflow.
- iPhone 16 Pro replay at 402×874: the ledger measured 356×237px at
  `(23, 318.5)`, its overlay background was transparent, no sky layer existed,
  and the reconciled result remained visible at 2.5 and 3.7 seconds before the
  transition released by 5.4 seconds.
- Deployment verification caught the canonical Pages host serving an older JS
  body under a reused default `index-[hash].js` URL while the immutable preview
  host served the new body. The web build now uses an explicit `dyor-[hash].js`
  entry namespace, preventing the old canonical cache entry from being reused.

### Verification

- `npm run typecheck`: PASS.
- Engine tests: 150/150 PASS.
- Browser: iPhone 16 Pro center-only settlement, stable table context and timed
  result hold PASS.

## Authoritative bank, pets, recurring market and match exit

- Fixed the recap contradiction after bank payoff. The bank screen and repayment
  already operated on Bank liabilities, but `scoreBreakdown.bankDebt` summed
  every remaining liability. Bank matching is now case-normalized and the recap
  excludes university/profession obligations from the bank line.
- Replaced the client-authored pet payload with a shared canonical catalog. A
  purchase sends only `petId`; the engine owns price, upkeep, one-pet enforcement
  and recurring stress/trust/income behavior. Exact pet identity survives server
  snapshots, appears beside the player's table portrait and gets a named line in
  the monthly settlement ledger.
- Replaced the permanent duplicated business shop with authoritative
  `businessMarket` state. Three shared, consumable offers open in rounds
  1/3/5/…; closed and unavailable IDs reject in the engine. The eleven rendered
  businesses now live in one catalog. Seeded tier rotation guarantees each open
  window contains a reachable, middle and ambitious price while surfacing the
  complete catalog over four windows.
- Settings places match actions before ordinary preferences. `Сдаться` applies
  an authoritative elimination and opens Recap; `Выйти из игры` is a distinct
  best-effort room leave that revokes a live resume token, hands the seat to a bot
  and returns the local player to Lobby even if the websocket is already down.
  Both use the shared cancel-first confirmation dialog.
- Browser replay found that an explicit exit from a `?autostart=1` quick-play
  URL immediately started a replacement match. Exit now removes that one-shot
  query flag before showing Lobby.

### Browser evidence at 402×874

- Round 1 showed exactly three shared offers: one affordable, one middle and one
  ambitious. Buying `Киоск у метро` reduced cash by `$1,200`, increased displayed
  asset cashflow by `$230/mo`, and removed that offer when the market reopened.
- Round 2 showed no business cards and named round 3 as the next market.
- Market and Settings had zero horizontal overflow. Match return, surrender and
  leave were visible in the first viewport; both destructive actions opened the
  common `ConfirmDialog`.
- Confirmed surrender opened Recap. Confirmed leave removed `autostart` from the
  URL and remained in Lobby instead of silently starting a new match.
- Earlier in the same flow, buying Барбос rendered the compact dog companion and
  settlement showed `Барбос · Стресс −2/ход` while authoritative stress changed.

### Verification

- Engine: 159/159 PASS.
- Server: 10/10 PASS.
- Simulation invariants/oracle: 57/57 PASS.
- Web and server TypeScript: PASS.
- Production web build: PASS; the existing >500 KB chunk warning remains.
- `git diff --check`: PASS.

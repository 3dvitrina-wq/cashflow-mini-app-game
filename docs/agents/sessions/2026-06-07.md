# Session 2026-06-07

## Lobby Ambient Pass

Request:

- оживить картинку лобби;
- добавить пар от кофе;
- сделать свет от лампы слегка помаргивающим;
- оживить неоновую вывеску.

Actions:

- Проверил обязательный агентский маршрут:
  - `docs/agents/AGENT_WIKILINKS.md`
  - `docs/agents/AUTO_CONTEXT.md`
  - `docs/agents/AGENT_START_HERE.md`
  - `docs/agents/ACTIVE_TASK.md`
  - `docs/agents/WORKFLOW.md`
- Нашёл целевой экран и стили:
  - `apps/web/src/screens/LobbyScreen.tsx`
  - `apps/web/src/index.css`
- Добавил переиспользуемый слой `LobbyAmbientFx` для двух зон:
  - полноэкранный фон лобби;
  - витрина комнаты в открытом room-view.
- Добавил CSS-анимации и оверлеи:
  - тёплое мягкое мерцание лампы;
  - пульсацию и лёгкий buzz у неоновой вывески `DYOR`;
  - многослойный пар и дымку над кружкой.
- Учёл `prefers-reduced-motion`, чтобы эффекты можно было безопасно приглушить.
- Собрал web-приложение и проверил мобильный кадр через локальный preview + headless screenshot.

Verification:

- `npm run build --workspace=apps/web`
- `npm exec vite preview -- --host 127.0.0.1 --port 4173` (фактически открылся `127.0.0.1:4174`)
- `npx playwright install webkit`
- `npx playwright screenshot --device="iPhone 13" "http://127.0.0.1:4174/?lobby=1" /tmp/lobby-animated.png`

Decisions:

- Эффекты реализованы как лёгкие CSS-слои поверх существующего PNG, без замены исходной иллюстрации.
- Неон анимирует существующую вывеску `DYOR`, а не вводит новый объект в сцену.
- Пар от кофе усилен не только движущимися струйками, но и лёгкой постоянной дымкой, чтобы сцена читалась живее даже под плотными UI-слоями.

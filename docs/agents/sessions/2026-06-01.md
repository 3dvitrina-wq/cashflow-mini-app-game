# Session 2026-06-01

## Topic

New character reference intake and chroma-key asset generation setup.

## Actions

- Inspected existing generated character structure:
  - `apps/web/src/assets/generated/characters/<id>/manifest.json`
  - `turnaround/`, `emotions/`, `parts/`
- Reviewed canonical style docs:
  - `docs/second_brain/10_game_design/CHARACTER_VISUAL_STYLE_PROMPT.md`
  - `docs/second_brain/10_game_design/IMAGE_GEN_PROMPTS.md`
  - `docs/second_brain/00_inbox/ASSET_GEN_PROMPTS.md`
- Copied five PNG references from `/Users/dzmitrysiarou/Documents/png refs` into project-local character folders.
- Created per-character manifests and first-pass profile prompt files.
- Added native-alpha runbook, then deferred it after the founder chose chroma-key:
  - `docs/agents/prompts/CHARACTER_V2_NATIVE_ALPHA_PIPELINE.md`
- Added current chroma-key runbook:
  - `docs/agents/prompts/CHARACTER_V2_CHROMAKEY_PIPELINE.md`
- Generated first-pass profile portraits for all five new characters using the built-in image generator on flat green backgrounds.
- Converted all five source-green PNGs to RGBA alpha PNGs with `remove_chroma_key.py`.
- Created contact sheet:
  - `tmp/imagegen/character-v2-alpha/portrait-contact-sheet.png`
- Validated manifest JSON and source reference alpha metadata.
- Dry-ran one imagegen CLI command. It parsed correctly, but `OPENAI_API_KEY` was not set, so no API native-alpha generation was performed.

## Decisions

- Use the richer 2.5D toy-comic / matte clay-plastic detail level from the references.
- Keep bust/profile portraits close to source.
- Adapt full-body sprites to current DYOR proportions and accessory anchor needs.
- Use chroma-key source PNG plus local alpha extraction for this pass.
- Keep the `gpt-image-1.5` native transparent PNG path as a deferred alternative only.
- Do not connect these assets to real-money trading, real investment recommendations, real logos, or real public figures.

## Character Folders

- `apps/web/src/assets/generated/characters/burnout_clerk/`
- `apps/web/src/assets/generated/characters/deal_maven/`
- `apps/web/src/assets/generated/characters/whale_broker/`
- `apps/web/src/assets/generated/characters/street_hustler/`
- `apps/web/src/assets/generated/characters/fixer_consultant/`
- `apps/web/src/assets/generated/characters/grandma_collector/`
- `apps/web/src/assets/generated/characters/mad_fashion/`
- `apps/web/src/assets/generated/characters/korean_student/`
- `apps/web/src/assets/generated/characters/campus_student/`
- `apps/web/src/assets/generated/characters/sky_pilot/`
- `apps/web/src/assets/generated/characters/police_officer/`
- `apps/web/src/assets/generated/characters/flight_attendant/`
- `apps/web/src/assets/generated/characters/vibe_coder/`

## Additional Reference Batch

The founder added more references to `/Users/dzmitrysiarou/Documents/png refs` and requested:

- grandma / `grandma_collector`
- crazy fashion / `mad_fashion`
- Korean student girl / `korean_student`
- student / `campus_student`
- pilot / `sky_pilot`
- police officer / `police_officer`
- stewardess / `flight_attendant`
- vibe coder / `vibe_coder`

Generated all eight first-pass profile busts using the chroma-key pipeline and local alpha extraction.

QA preview:

- `tmp/imagegen/character-v2-alpha/portrait-contact-sheet-refs-2.png`

## Next Step

Review the generated profile portraits, then generate one approved character's turnaround set before producing all remaining turnaround and emotion states.

## Role Pass: Rapper, Cashier, Teacher

The founder requested three more characters from `/Users/dzmitrysiarou/Documents/png refs`:

- Rapper woman: `rap_queen`
- Cashier woman: `checkout_cashier`
- Teacher woman: `classroom_teacher`

Actions:

- Reused existing `rap_queen` folder and added profile prompt plus two variant references.
- Added `checkout_cashier` folder with `references/`, `prompts/`, `portraits/`, `turnaround/`, `emotions/`, and `parts/`.
- Added `classroom_teacher` folder with the same structure plus an alternate blonde hair/style reference.
- Added per-character manifests with shared anchors and `variantSlots` so hair, outfit pieces, glasses, jewelry, and handheld props can become swappable later.
- Updated the chroma-key pipeline with this role pass.
- Generated first-pass profile busts for all three using the built-in image generator on flat green backgrounds.
- Converted all three source-green PNGs to RGBA alpha PNGs with `remove_chroma_key.py`.
- Created role-pass QA contact sheet:
  - `tmp/imagegen/character-v2-alpha/portrait-contact-sheet-role-pass.png`

Generated alpha files:

- `apps/web/src/assets/generated/characters/rap_queen/portraits/rap_queen_profile_bust.png`
- `apps/web/src/assets/generated/characters/checkout_cashier/portraits/checkout_cashier_profile_bust.png`
- `apps/web/src/assets/generated/characters/classroom_teacher/portraits/classroom_teacher_profile_bust.png`

QA result:

- All three final portrait PNGs are `1024x1536` RGBA.
- All four image corners have alpha `0`.

## Model Sheet Slicing Pass

Founder redirected the work from more generation to slicing and game intake preparation:

- Stop generating new character sheets.
- Slice all newly generated `*-model-sheet-alpha.png` assets into per-character game folders.
- Add a separate prompt for a fresh context window that will wire the characters into settings, shop, and game selection.
- Keep detailed portrait busts/references in a separate portrait box for profile pages and shop carousel use.

Added integration handoff prompt:

- `docs/agents/prompts/CHARACTER_GAME_IMPORT_PROMPT.md`

Added slicing tool:

- `tools/slice_character_model_sheets.py`

Sliced characters:

- `artist` / `Художник`
- `burnout_clerk` / `Уставший клерк`
- `campus_student` / `Мажор-студент`
- `checkout_cashier` / `Кассирша`
- `classroom_teacher` / `Учительница`
- `deal_maven` / `Переговорщица`
- `fixer_consultant` / `Консультантка`
- `flight_attendant` / `Стюардесса`
- `grandma_collector` / `Бабка`
- `korean_student` / `Студентка`
- `mad_fashion` / `Мажор`
- `police_officer` / `Полицейский`
- `rap_queen` / `Реперша`
- `sky_pilot` / `Летчик`

Per-character output folders now include:

- `turnaround/` - front, three-quarter, side, back three-quarter, back.
- `emotions/` - stable, overworked, overleveraged, tax panic, work crisis, `futures_liq` alias, passive calm, cardboard, nomad.
- `parts/` - best-effort part crops from the sheet lower row.
- `portrait_refs/` - final profile portrait plus source/variant references.

Added index:

- `apps/web/src/assets/generated/characters/character-index.json`

QA preview:

- `tmp/imagegen/character-v2-alpha/sliced-character-contact-sheet.png`

QA note:

- Full-body game states are clean enough for game/shop intake.
- `parts/` are best-effort because generated sheets do not place lower-row parts in a perfectly consistent order. The full `*-model-sheet-alpha.png` and source boxes remain in each manifest for repeat/manual slicing.

## Character UI Intake Pass

Request:

- Run the app locally so the founder can inspect current state.
- Put generated characters into game slots and the shop.
- Verify characters do not visually drift outside their frames in the shop or on the game table.

Actions:

- Added `apps/web/src/assets/generatedCharacterCatalog.ts` as the UI catalog for the 14 sliced, game-ready characters.
- Wired generated characters through `characterId` while keeping the existing six `Outfit` values for deterministic engine compatibility.
- Updated starter lobby/game roster to show generated character portraits and full-body avatars.
- Added a `characters` shop tab and direct QA route `?shop=1`.
- Used portrait busts in compact player slots and full-body stable PNGs in larger player scenes.
- Tightened slot/shop image sizing with `object-fit: contain`, `object-position: center bottom`, and clipped shop art wells.

QA:

- `npm run typecheck` passed.
- Started Vite locally at `http://127.0.0.1:5173/`.
- Captured mobile screenshots for `?autostart=1` and `?shop=1`.
- Visual result: shop character cards stay inside their art frames; top game slots now use centered portrait busts instead of full-body transparent-sheet crops; the larger player panel keeps the full-body character inside its frame.

Follow-up editor fix:

- Founder screenshot showed the character editor still using legacy role art and letting the preview character overlap the info panel.
- Replaced the editor role grid with the generated-character catalog.
- Added direct QA route `?editor=1`.
- Saved `characterId` with player data while keeping `outfit` as the six-value engine-safe role.
- Tightened editor preview/card CSS so character art is clipped to the preview frame and centered bottom.
- Re-ran `npm run typecheck` successfully.
- Captured mobile screenshot for `?editor=1`; preview, info card, tabs, and character grid no longer overlap.

## YOU Panel Visual Hierarchy Pass

Request:

- Rework the in-game character menu/player panel because the layout and proportions felt weak.
- Make the hero portrait larger, pseudo-volumetric, and visually breaking out of a rigid frame.
- Remove low-value hierarchy like `HUSTLER LVL 12` as the main focus.
- Prioritize money, debt, expenses, stress, business slots, and protections.
- Put some important status directly on or around the figure.

Actions:

- Rebuilt the `YOU` panel layout in `MainTurnTableScreen.tsx`.
- Replaced the framed avatar box with a halo/shadow stage and oversized full-body hero.
- Moved cash, monthly flow, debt, and stress meter into floating badges around the character.
- Replaced the old level header with player name plus `баланс хода`.
- Added compact financial cards for cash, flow, and expenses.
- Added a risk row for stress, debt, and passive income.
- Kept business slots and protection icons as secondary inventory blocks.
- Updated responsive CSS for the short-height mobile layout.

QA:

- `npm run typecheck` passed.
- Captured mobile screenshot for `?autostart=1`: `/tmp/dyor-you-panel-redesign.png`.
- Visual result: hero is larger and dimensional, the old role/level emphasis is gone, and practical financial state is now the primary read.

## Lobby Visual Flow Pass

Request:

- In the lobby, move `Start` and invite actions directly after the player list instead of leaving them at the bottom.
- Move match settings out of the room view and into settings.
- Make the first lobby screen a clear choice: join a room or create your own.

Actions:

- Added a first lobby choice state with `Присоединиться` and `Создать свою`.
- Moved the room action buttons directly below the current players and above empty invite slots.
- Removed the match settings panel from the room view.
- Added `Настройки матча` controls to `SettingsScreen`.
- Made lobby settings open as a full-screen local settings view so closing returns to the same lobby room state.
- Added explicit global `openSettings(returnTo)` support for main-table settings navigation.

QA:

- `npm run typecheck` passed.
- `npm run build --workspace=apps/web` passed.
- Local smoke on `http://localhost:5174/` passed for onboarding → lobby choice → create room → settings → close back to room.
- Visual screenshots: `/tmp/dyor-lobby-entry.png`, `/tmp/dyor-lobby-room.png`, `/tmp/dyor-settings.png`.

Follow-up:

- Founder clarified that empty `+ invite` slots are part of the player-slot stack, so primary actions should be below them.
- Moved `Invite link` and `Start` under the two empty slots.
- Re-ran `npm run typecheck` successfully.
- Captured `/tmp/dyor-lobby-slots-before-actions.png`; smoke confirmed action buttons are below the first empty slot.

## Character Editor Sticky Preview Pass

Request:

- The character editor preview should stay visible while scrolling down through the character list.

Actions:

- Changed the editor shell to a fixed app-height flex layout with hidden outer overflow.
- Moved scrolling responsibility to `.editor-scroll-panel`, the lower list/content area.
- Kept preview, emotion strip, info card, tabs, and save action outside the scrolling panel so the selected character remains visible.

QA:

- `npm run typecheck` passed.
- `npm run build --workspace=apps/web` passed.
- Local smoke on `?editor=1`: after scrolling `.editor-scroll-panel`, preview Y position stayed fixed while character cards moved.
- Visual screenshot: `/tmp/dyor-editor-sticky-preview.png`.

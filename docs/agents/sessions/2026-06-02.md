# Session 2026-06-02

## Table HUD And Reactions Polish

Request:

- Make `YOU` panel financial text and numbers larger and better centered.
- Center the `cash / flow / expenses / protection` reads within their blocks.
- Fix the bottom confirm button being visually cut by the app layout.
- Move/rename compact sticker reactions because they were not in the right place.

Actions:

- Enlarged primary `YOU` panel financial numbers and centered stat-card content.
- Centered business slot and protection mini-panels.
- Removed word labels from the small stress/debt/passive pills, leaving icon + value to avoid clipped text.
- Kept `ДЕНЬГИ`, `ПОТОК`, and `РАСХОДЫ` visible in the primary stat cards.
- Renamed the stickers control to `РЕАКЦИИ`.
- Converted reactions from a lower drawer into a compact centered pull pill between the event card and `YOU` panel.
- Reactions now open upward from the pill and do not consume extra vertical layout space.
- Reduced bottom action dock padding so the confirm button stays inside the viewport.

QA:

- `npm run typecheck` passed.
- `npm run build --workspace=apps/web` passed.
- Playwright smoke on `?autostart=1` confirmed the confirm button bottom is inside the 390x844 viewport.
- Visual screenshots:
  - `/tmp/dyor-hud-reactions-polished.png`
  - `/tmp/dyor-you-reactions-final-open.png`
  - `/tmp/dyor-you-reactions-final-closed.png`

## Player Character Sheet V2

Request:

- Rework the player character screen opened from the main table so it matches the 2.5D comic game style.
- Split crowded stats into swipeable sections.
- Keep future features visible: assistants, pets, housing/mansion, state, main portrait.
- Add distinct pet staging for fish and parrot.

Actions:

- Rebuilt `PlayerStatsScreen` as a character hub with tabs/swipe pages: status, finance, home, crew, style.
- Kept the main portrait as a staged diorama with housing backdrop, foreground avatar, and pet platform.
- Added `PetStage` rendering:
  - fish uses the existing round aquarium container plus fish cutout;
  - parrot uses a perch platform;
  - ground pets use a floor pad.
- Moved dense metrics into the finance page and kept status page to cash, flow, stress, condition, and feature slots.
- Added crew page placeholders for assistants and bots without changing engine rules.

QA:

- `npm run build` passed in `apps/web`.
- Temporary Playwright check on `?autostart=1&you=1` confirmed fish and parrot render in the player sheet.
- Visual screenshots:
  - `tmp/player-profile-status-fish.png`
  - `tmp/player-profile-crew-fish-parrot.png`

## Player Character Sheet Reference Match

Request:

- Make the first implemented character sheet match the second visual reference more closely.
- Redraw the scene, icons, spacing, and feature-card composition instead of keeping the old current look.

Actions:

- Reworked the header into the reference layout: back circle, large name, role pill, edit circle.
- Replaced tab text symbols with custom/local icon treatment and active underline.
- Rebalanced the hero stage: stronger room background, character pedestal, pet pedestal, and softer scene lighting.
- Rebuilt the status alert as a red bordered tactical panel with warning icon and tax-paper side art.
- Replaced plain feature slots with image cards for home, pet, assistant, and protection.
- Preserved fish aquarium and parrot perch handling from the previous pass.

QA:

- `npm run build` passed in `apps/web`.
- Temporary Playwright visual check saved `tmp/player-profile-v3-gecko-final.png`.

## Player Profile PNG Scene Asset

Request:

- Correct the scene implementation: the room scene should be drawn as PNG assets, not simulated with CSS shapes.

Actions:

- Generated a production PNG room scene with empty character/pet platforms.
- Copied the final scene into `apps/web/src/assets/generated/profile-scenes/starter-room-character-scene.png`.
- Switched `PlayerStatsScreen` hero scene to use that PNG as the background layer.
- Removed the active CSS room-object overlay from the main scene; dynamic character and pet cutouts remain layered over the PNG.
- Kept fish aquarium/parrot perch logic for pet-specific overlays.

QA:

- `npm run build` passed in `apps/web`.
- Temporary Playwright visual check saved `tmp/player-profile-png-scene-gecko-final.png`.

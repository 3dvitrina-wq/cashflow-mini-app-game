# Финальный отчёт: баги онлайн-игры + хардкод

Дата: 2026-06-07. Источник: трассировка движка на партию 6 игроков (агент-дебаггер) + анализ залипательности (агент-наблюдатель). Все пункты привязаны к коду.

Детали: [DIAGNOSIS.md](DIAGNOSIS.md), [ENGAGEMENT.md](ENGAGEMENT.md).

## Статус: ВСЁ ИСПРАВЛЕНО И ПРОВЕРЕНО (2026-06-07)

Движок: 139 тестов passed. Веб: `npm run build` ✓, `tsc --noEmit` 0 ошибок.

Ключевая первопричина английских карт: `packages/game-engine/src/i18n.ts` был **обрезан с первого WIP-коммита** (заканчивался посреди `EFFECT_DESCRIPTIONS_RU` без закрывающей скобки). Сборку это не ломало только потому, что файл **никто не импортировал** - и ровно поэтому `getLocalizedCard()` никогда не вызывался, карты шли по-английски. Файл дозакрыт, импорт подключён.

| Пункт | Файл | Статус |
|-------|------|--------|
| 6 Зависание хода | apps/server/src/index.ts | ✅ таймер пере-армится в ветке ошибки с guard от двойного |
| 5/1 Англ. карты | store/index.ts + i18n.ts | ✅ getLocalizedCard в toUiCard + 5 futures-переводов + обрыв файла починен |
| 3 Партнёрство | engine.ts formPartnerships | ✅ дебет с каждого партнёра по shareRules, без двойного списания |
| 2 Кто кому платит | DealModalScreen.tsx | ✅ живой расчёт стоимости сделки на игрока вместо баланса |
| 4 Слайдер доплаты | OfferBuilderModal.tsx | ✅ подпись-объяснение под слайдером |
| H Фейк-офферы | DealModalScreen.tsx | ✅ источник - реальный `incomingDeal` из матча; нет офферов = честный idle (фейк @lex/@nika не рендерится) |
| H Seed | store/index.ts | ✅ рандом per-match (офлайн), онлайн-детерминизм не затронут |
| H Таймер 00:47 | DealModalScreen.tsx | ✅ заменён на реальный `expiresRound` |
| H Кнопка Collab | CollaborationHubScreen.tsx | ✅ шлёт реальный `submitDealOffer` |
| H Награда за вход | DailyCardScreen.tsx | ✅ начисляется через `savePlayerData` |
| Туториал | TutorialOverlay.tsx (new) | ✅ 6 коучмарков, localStorage-gate, монтаж в MainTurnTableScreen |
| Залипательность | MainTurnTableScreen.tsx | ✅ A1 живой wait-panel+реакции, A3 +/-$ под выбором, A4 баннер для зрителей |

Отложено как предложения (риск для дизайна): A2 вирусный recap, A5 голосование в лобби, A6 host-nudge. Фейковый массив `DEAL_PROPOSALS` в `data/deals.ts` остался в файле, но больше нигде не импортируется (мёртвый, безвредный) - можно удалить отдельным коммитом.

## P0 - ломает игру / отгружается сломанным юзеру

| # | Баг | Корень | Фикс |
|---|-----|--------|------|
| 6 | **Зависание хода.** Игрок ждёт чужой ход, не разрешается даже после таймера | `server/index.ts:267-276` - при ошибке команды `clearTurnTimer`, затем `drainBotTurns()` выходит сразу (ход человека), новый таймер не ставится. Матч висит до heartbeat (35с) | После `drainBotTurns` в ветке ошибки: если `!r.turnTimer` - поставить `setTimeout(TURN_TIMEOUT_MS)` |
| 5/1 | **Карты по-английски + "разные у всех".** Карта одна (движок ок), но рендер английский | `store/index.ts:208` - `toUiCard()` читает `card.title`/`card.text` напрямую, минуя `getLocalizedCard()`. + 5 futures-карт нет в `CARD_LOCALES_RU` (`i18n.ts`) | (a) один вызов `getLocalizedCard(card.id)` в `toUiCard()`; (b) добавить 5 переводов |
| 3 | **Партнёрство не списывает со всех.** Платит только тот, кто нажал "Co-invest" | `effects.ts:287` + `engine.ts:1077` `formPartnerships()` без дебета | Дебетовать cash с каждого партнёра пропорц. `shareRules`, если не платил через `partnership.invite` |
| H-b | **Фейковые офферы от несуществующих ботов** (@lex, @nika...) | `deals.ts:72` - 8 выдуманных сделок, `DealModalScreen` крутит по `round % 8`, к движку не привязаны | Заменить на реальные офферы из движка / убрать |

## P1 - UX-корректность

| # | Баг | Корень | Фикс |
|---|-----|--------|------|
| 2 | **Не видно кто кому сколько платит.** Возле аватара - баланс, не стоимость сделки | `DealModalScreen.tsx:105` - `PnL` рендерит `cashflowPerMonth`, не стоимость текущей сделки | Считать `assetValue * share/100` на игрока, отдавать в `PnL` живой расчёт |
| 4 | **Слайдер "доплата партнёру" без объяснения** | `OfferBuilderModal.tsx:82-88` - `cashOffer = sidePayment`, разовый кэш-бонус proposer→partner (max $500 / 20% кэша) | Не убирать. Добавить подпись-объяснение под слайдером |

## Хардкод-охота (прочее)

- `store/index.ts:366` - фиксированный seed `20260529`: одинаковая колода каждую офлайн-игру
- `DealModalScreen.tsx:189` - таймер намертво `00:47`
- `CollaborationHubScreen.tsx:42-50` - "Отправить предложение" показывает тост, команду в движок не шлёт
- `DailyCardScreen.tsx:36-38` - награда за вход показывается, но не начисляется

## Параллельные улучшения (виральность + онбординг)

Залипательность: A1 мёртвый экран ожидания, A2 невирусный recap, A3 скрытое превью выбора, A4 зрители не видят переговоры, A5 скучное lobby-ожидание, A6 редкий AI-host.
Туториал: 6 коучмарков поверх первого матча (карта → выбор → превью "?" → твои цифры → кнопка "+" → реакции). Встройка: `TutorialOverlay.tsx` в `MainTurnTableScreen.tsx:1293`.
